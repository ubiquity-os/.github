import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "UbiquityOS Sprint Manager — AI Sprint Planning for Engineering Teams",
  description:
    "Connect your GitHub org and let AI plan your sprints. Auto-prioritize tasks, estimate durations, assign work, and see ROI metrics in real time.",
  openGraph: {
    title: "UbiquityOS Sprint Manager",
    description: "AI-powered sprint planning that saves engineering teams 12+ hours per sprint.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
